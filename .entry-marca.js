// Punto de entrada solo para generar herramientas/vendor/material-color.js
// (npm run vendor:marca). Lo usa el generador de marca, no las páginas.
export { argbFromHex, hexFromArgb, themeFromSourceColor, sourceColorFromImage,
         TonalPalette, Hct } from '@material/material-color-utilities';
